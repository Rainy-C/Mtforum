# MT论坛 Flutter 客户端 - 开发进度文档

## 项目概述
- **项目名**: mtforum
- **框架**: Flutter 3.27.0 + Dart 3.6.0
- **UI**: Material Design 3 (ColorScheme.fromSeed, 种子色 #1565C0)
- **目标**: MT管理器论坛 (bbs.binmt.cc) 第三方客户端
- **论坛系统**: Discuz! X3 + comiis_app_portal 移动版插件
- **编译服务器**: 159.195.34.148 (root / 5A1GrPE9lYhkSoo)
  - Flutter: /opt/flutter
  - Android SDK: /opt/android-sdk
  - 项目路径: /root/mtforum
  - APK下载: http://159.195.34.148:8080/mtforum.apk

---

## 已完成功能 (✅)

### 1. 帖子列表 (首页)
- [x] AJAX接口加载帖子列表 (`plugin.php?id=comiis_app_portal&...&inajax=1`)
- [x] 3个Tab: 热门/最新/精华 (目前Tab切换不换数据，需修)
- [x] 下拉刷新 + 上拉加载更多
- [x] 显示: 标题、摘要、作者、头像、版块标签、阅读量、评论量、点赞量、时间
- [x] HTML解析 (forumlist_li 结构)

### 2. 帖子详情页
- [x] 帖子内容 + 楼层列表
- [x] 楼主标记 (第1页第1楼) + 楼层号 (楼主/沙发/板凳/地板/N楼)
- [x] 富文本渲染: 文字、加粗、链接、表情图、帖子图片、引用块、代码块
- [x] 隐藏内容提示 (🔒卡片显示 "XXX，如果您要查看本帖隐藏内容请回复")
- [x] 回复引用指示 ("回复 @用户名")
- [x] 图片点击全屏查看 (photo_view, 支持缩放/滑动)
- [x] 点赞按钮 (底部操作栏)
- [x] 收藏按钮 (底部操作栏 + AppBar)
- [x] 回复功能 (BottomSheet弹窗, 支持引用回复 repquote)
- [x] 分页加载 + pid去重
- [x] DOM解析失败时正则兜底解析

### 3. 搜索
- [x] 3步搜索流程 (POST→302拿searchid→GET结果页)
- [x] 搜索结果卡片 (标题、摘要、作者、版块、时间)
- [x] 翻页加载更多

### 4. 个人中心
- [x] 用户信息卡 (头像、用户名、UID)
- [x] 积分/金币显示
- [x] 功能菜单 (我的帖子/收藏/好友/签到排行 - 前三个仅占位)
- [x] 签到排行页面 (今日/本月/总排行 Tab)

### 5. 登录系统
- [x] 账号密码登录 (member.php?mod=logging&action=login)
- [x] Cookie登录 (手动填auth+saltkey, 折叠区)
- [x] 登录状态回调机制 (addLoginListener)
- [x] 测试连接功能
- [x] 清除凭证

### 6. 基础设施
- [x] Dio + Cookie管理 (cookie_jar + dio_cookie_manager)
- [x] SharedPreferences 持久化登录凭证
- [x] MD3 亮色/暗色主题
- [x] NavigationBar 底部导航 (4页)
- [x] SliverAppBar 大标题折叠

---

## 已知问题 / 待修复 (⚠️)

### 高优先级
1. **首页Tab切换不换数据** - 热门/最新/精华三个Tab目前加载相同数据
   - 原因: `getThreadList` 没有根据Tab传不同参数
   - 修复: 热门=`view=hot` 最新=`view=newthread` 精华=`view=digest`，需要改用 `forum.php?mod=guide&view=xxx` 整页解析 或找对应AJAX接口

2. **帖子内容解析仍不稳定** - 部分帖子内容可能提取不全
   - 已加正则兜底但仍可能有遗漏
   - 建议用更稳健的HTML解析策略，或考虑用 `flutter_html` 包直接渲染原始HTML

3. **登录后需手动刷新** - 登录成功后首页帖子列表不会自动重新加载
   - 需要在登录回调里触发首页刷新

### 中优先级
4. **我的帖子/收藏/好友** - 菜单项点击仅显示"功能开发中"，未实现页面
5. **发新帖** - 未实现 (用户说帖子发了删不掉，暂时搁置)
6. **实际签到操作** - 未实现 (用户已签过，暂时搁置)
7. **私信系统** - 接口已抓到但未实现UI

### 低优先级
8. **帖子详情的BBCode** - Discuz返回的是HTML不是BBCode，但部分内容可能解析不完美
9. **图片加载优化** - 没有图片缓存策略
10. **错误处理** - 网络错误提示不够友好

---

## 接口文档

完整接口文档见同目录 `binmt_api_doc.md`，共34个接口，包括:
- 帖子列表/详情/回复/引用回复/点赞
- 搜索 (3步流程)
- 个人中心/积分/他人主页
- 私信查看/发送
- 签到排行/积分商城
- 账号登录 (无验证码)

### 核心参数
- **登录态Cookie**: `cQWy_2132_auth` + `cQWy_2132_saltkey`
- **formhash**: 写操作必需，从页面HTML提取 (`formhash = '([a-f0-9]+)'`)
- **noticeauthor**: 回复帖子必需，从帖子详情页提取
- **searchid**: 搜索POST后从302 Location头提取
- **返回格式**: AJAX接口返回XML `<root><![CDATA[HTML片段]]></root>`，需取CDATA内容

---

## 项目结构

```
mtforum/
├── lib/
│   ├── main.dart                      # 入口 + 底部导航 (4页)
│   ├── theme/
│   │   └── app_theme.dart             # MD3 主题 (亮色/暗色)
│   ├── models/
│   │   └── models.dart                # 数据模型 (Thread/Post/PostContent/UserProfile/SearchResult)
│   ├── services/
│   │   └── api_service.dart           # API服务 (全部接口 + HTML解析)
│   └── pages/
│       ├── home_page.dart             # 首页-帖子列表
│       ├── thread_detail_page.dart    # 帖子详情+回复+点赞+收藏+图片查看
│       ├── search_page.dart           # 搜索
│       ├── profile_page.dart          # 个人中心+签到排行
│       ├── settings_page.dart         # 设置-登录配置
│       └── login_page.dart            # 账号密码登录
├── pubspec.yaml                       # 依赖配置
├── binmt_api_doc.md                   # 完整接口文档
└── android/                           # Android配置 (含INTERNET权限)
```

### 依赖包
```yaml
dio: ^5.7.0                    # HTTP客户端
cookie_jar: ^4.0.8             # Cookie管理
dio_cookie_manager: ^3.1.1     # Dio Cookie拦截器
shared_preferences: ^2.3.3     # 本地存储
html: ^0.15.4                  # HTML解析
cached_network_image: ^3.4.1   # 图片缓存
url_launcher: ^6.3.1           # 打开链接
photo_view: ^0.15.0            # 图片缩放查看
pull_to_refresh: ^2.0.0        # 下拉刷新 (未使用)
google_fonts: ^6.2.1           # 字体 (未使用)
```

---

## 关键代码位置

| 功能 | 文件 | 关键方法 |
|------|------|----------|
| 帖子列表解析 | api_service.dart | `_parseThreadList()` |
| 帖子详情解析 | api_service.dart | `getThreadDetail()` + `_parseThreadDetailFallback()` |
| 富文本解析 | api_service.dart | `_parseRichContent()` |
| 搜索3步流程 | api_service.dart | `search()` |
| 账号登录 | api_service.dart | `login()` |
| 回复帖子 | api_service.dart | `replyThread()` |
| 登录状态回调 | api_service.dart | `addLoginListener()` / `_notifyLoginChanged()` |
| 图片全屏查看 | thread_detail_page.dart | `_FullScreenImageViewer` |
| 回复弹窗 | thread_detail_page.dart | `_ReplySheet` |

---

## 编译方法

```bash
# 在编译服务器上
export PATH=$PATH:/opt/flutter/bin
export ANDROID_HOME=/opt/android-sdk
cd /root/mtforum
flutter pub get
flutter build apk --release
# 输出: build/app/outputs/flutter-apk/app-release.apk

# 开HTTP下载服务
cp build/app/outputs/flutter-apk/app-release.apk /root/mtforum.apk
cd /root && python3 -m http.server 8080
```

---

## 下一步建议

1. **修复Tab切换** - 让热门/最新/精华加载不同数据
2. **用 flutter_html 渲染** - 替换手动富文本解析，更稳定
3. **实现我的帖子/收藏页面** - 接口已抓到
4. **登录后自动刷新首页** - 在loginState回调里触发
5. **私信UI** - 接口已抓到
6. **签到功能** - 等用户能签到了再抓接口补全
